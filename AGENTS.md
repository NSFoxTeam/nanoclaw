## Review guidelines

- Check for security issues: no hardcoded secrets, no SQL injection, no XSS
- Verify error handling: API calls should handle failures gracefully
- Ensure code follows existing project conventions and patterns
- Flag any breaking changes to public APIs
- Check that new dependencies are justified and well-maintained
